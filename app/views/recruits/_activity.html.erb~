<table>
  <tr>
    <% ['', 'Scheduled For', 'Completed At'].each do |header| %>
      <th><%= header %></th>
    <% end %>
  </tr>
  <% @recruit.activities.each do |activity| %>
    <tr>
      <td><%= activity.friendly_name %></td>
      <td><%= activity.scheduled_for || "Pending" %></td>
  <% end %>
<table>
